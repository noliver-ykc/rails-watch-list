<button type="button" class="mt-2 btn btn-dark"><%= link_to "←", lists_path %></button>
<h1><%= @list.name %></h1>
<% @list.bookmarks.each do |bookmark| %>
  <div class="card mb-3" style="max-width: 540px;">
    <div class="row g-0">
      <div class="col-md-4">
        <img src="<%= bookmark.movie.poster_url %>" class="img-fluid rounded-start" alt="...">
      </div>
      <div class="col-md-8">
        <div class="card-body">
          <h5 class="card-title"><%= bookmark.movie.title %></h5>
          <p class="card-text"><%= bookmark.comment %></p>
          <%= link_to "🗑", bookmark_path(bookmark.id), method: :delete, data: { confirm: "Are you sure?" } %>
        </div>
      </div>
    </div>
  </div>
<% end %>
<button type="button" class="btn btn-dark"><%= link_to "＋ Bookmark", new_list_bookmark_path(@list) %></button>
